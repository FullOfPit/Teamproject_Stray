export default function FindLocationOverlay() {
    // show list of search results
    // show map, that shows marker for selected searchResult
    // cancel button -> close overlay -> do nothing
    // add button -> close overlay -> add location to trip -> show location marker on map

    return (
        <>
            <p>I am the find location overlay</p>
        </>
    )
}