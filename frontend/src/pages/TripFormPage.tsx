import "./TripFormPage.css";

export default function TripFormPage() {
    // show input for title
    // show map with markers for all added locations
    // show AddLocationForm
    // save button -> on click -> POST to api
    // if post successful -> navigation to DashboardPage ?

    return (
        <>
            <h1>TripFormPage</h1>
        </>
    );
}